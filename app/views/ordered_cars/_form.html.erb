<%= form_with(model: ordered_car, local: true) do |form| %>
  <% if ordered_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ordered_car.errors.count, "error") %> prohibited this ordered_car from being saved:</h2>

      <ul>
      <% ordered_car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :car_id %>
    <%= form.text_field :car_id, id: :ordered_car_car_id %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id, id: :ordered_car_client_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :ordered_car_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

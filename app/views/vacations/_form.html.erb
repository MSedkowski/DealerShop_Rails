<%= form_with(model: vacation, local: true) do |form| %>
  <% if vacation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vacation.errors.count, "error") %> prohibited this vacation from being saved:</h2>

      <ul>
      <% vacation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id, id: :vacation_employee_id %>
  </div>

  <div class="field">
    <%= form.label :beginning_date %>
    <%= form.date_select :beginning_date, id: :vacation_beginning_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date, id: :vacation_end_date %>
  </div>

  <div class="field">
    <%= form.label :how_many_days %>
    <%= form.number_field :how_many_days, id: :vacation_how_many_days %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
